---
import BaseLayout from "@/layouts/BaseLayout.astro";
import LocalizedContactPage from "@/components/pages/LocalizedContactPage.astro";
import { localizedContactContent } from "@/data/localizedContactContent";

const content = localizedContactContent.en;
---

<BaseLayout locale="en" title={content.seoTitle} description={content.seoDescription}>
  <LocalizedContactPage content={content} locale="en" />
</BaseLayout>
